# Setting up Travis CI with Docker to allow building and testing on the latest
# version of Ubuntu which is currently 17.04.
sudo: required

language: generic

services:
  - docker

before_install:
  - docker pull ubuntu:latest
  - docker ps -l


install:
  - docker run ubuntu bash -c "apt-get update"
  - docker run ubuntu bash -c "apt-get install -y gcc git swig python-setuptools gettext python-dev python-numpy libgtk-3-dev python-gi-dev libpng-dev liblcms2-dev libjson-c-dev gir1.2-gtk-3.0 python-gi-cairo"

before_script:
  - docker run ubuntu bash -c "git clone https://github.com/mypaint/libmypaint"
  - docker run ubuntu bash -c "cd libmypaint"
  - docker run ubuntu bash -c "./autogen.sh"
  - docker run ubuntu bash -c "./configure --prefix=/usr"
  - docker run ubuntu bash -c "make"
  - docker run ubuntu bash -c "sudo make install"
  - docker run ubuntu bash -c "cd .."
  - docker run ubuntu bash -c "sudo rm -fr libmypaint"

script:
  - docker run ubuntu bash -c "python setup.py build"
  - docker run ubuntu bash -c "python setup.py nosetests --tests lib"
  - docker run ubuntu bash -c "python setup.py test"
  - docker run ubuntu bash -c "sudo python setup.py managed_install"
  - docker run ubuntu bash -c "sudo python setup.py managed_uninstall"
  - docker run ubuntu bash -c "sudo python setup.py install --root=/app/ --prefix=."
  - docker run ubuntu bash -c "bash -ex appimage/appimage.sh || true"
